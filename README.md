# Универсальный рендерер форм

## Описание

Реализована универсальная система для создания и отображения различных типов заявок на основе JSON-конфигурации. Система полностью модульная и расширяемая.

## Архитектура

### Модули системы

1. **form-configs.js** - Конфигурации всех типов форм
2. **form-renderer.js** - Универсальный рендерер форм
3. **validator.js** - Валидация полей
4. **data-service.js** - Работа со справочниками и отправка данных
5. **app.js** - Главная логика приложения
6. **styles.css** - Стили интерфейса

### Поддерживаемые типы полей

- **text** - Текстовое поле
- **email** - Email поле с валидацией
- **number** - Числовое поле с min/max
- **date** - Дата
- **time** - Время
- **select** - Выпадающий список
- **textarea** - Многострочный текст

## Как добавить новый тип заявки

### Шаг 1: Добавить тип формы в form-configs.js

```javascript
const formTypes = [
    // ...существующие типы
    {
        id: 'new-request',           // Уникальный ID
        name: 'Новая заявка',        // Название для UI
        description: 'Описание',      // Краткое описание
        icon: '📋'                    // Иконка (emoji)
    }
];
```

### Шаг 2: Создать конфигурацию формы

```javascript
const newRequestFormConfig = [
    {
        name: 'fieldName',           // Уникальное имя поля
        type: 'text',                // Тип поля
        label: 'Название поля',      // Подпись
        placeholder: 'Подсказка',    // Плейсхолдер
        required: true,              // Обязательное поле
        minLength: 3,                // Мин. длина (для text/textarea)
        maxLength: 100,              // Макс. длина (для text/textarea)
        min: 0,                      // Мин. значение (для number)
        max: 100,                    // Макс. значение (для number)
        dictionary: 'dictionaryName', // Название справочника (для select)
        dependsOn: 'parentField',    // Зависимость от другого поля
        rows: 4                      // Количество строк (для textarea)
    }
];
```

### Шаг 3: Добавить в маппинг

```javascript
const formConfigsMap = {
    // ...существующие
    'new-request': newRequestFormConfig
};
```

### Шаг 4: Добавить справочники (если нужно)

В файле **data-service.js**:

```javascript
const mockData = {
    // ...существующие справочники
    myNewDictionary: [
        { id: '1', name: 'Опция 1' },
        { id: '2', name: 'Опция 2' }
    ]
};
```

Для зависимых справочников:

```javascript
myDependentDictionary: {
    'parent-1': [
        { id: 'child-1', name: 'Дочерний элемент 1' }
    ],
    'parent-2': [
        { id: 'child-2', name: 'Дочерний элемент 2' }
    ]
}
```

## Примеры конфигураций

### Простое текстовое поле

```javascript
{
    name: 'userName',
    type: 'text',
    label: 'Имя пользователя',
    placeholder: 'Введите имя',
    required: true,
    minLength: 2,
    maxLength: 50
}
```

### Email с валидацией

```javascript
{
    name: 'email',
    type: 'email',
    label: 'Email',
    placeholder: 'example@mail.com',
    required: true,
    maxLength: 100
}
```

### Числовое поле с ограничениями

```javascript
{
    name: 'age',
    type: 'number',
    label: 'Возраст',
    placeholder: 'Введите возраст',
    required: true,
    min: 18,
    max: 100
}
```

### Выпадающий список

```javascript
{
    name: 'priority',
    type: 'select',
    label: 'Приоритет',
    placeholder: 'Выберите приоритет',
    required: true,
    dictionary: 'priorities'  // Справочник из data-service.js
}
```

### Зависимый выпадающий список

```javascript
{
    name: 'department',
    type: 'select',
    label: 'Отдел',
    placeholder: 'Выберите отдел',
    required: true,
    dictionary: 'departments',
    dependsOn: 'city'  // Зависит от выбранного города
}
```

### Многострочное текстовое поле

```javascript
{
    name: 'description',
    type: 'textarea',
    label: 'Описание',
    placeholder: 'Введите подробное описание',
    required: true,
    minLength: 10,
    maxLength: 1000,
    rows: 5
}
```

### Поле даты

```javascript
{
    name: 'startDate',
    type: 'date',
    label: 'Дата начала',
    required: true
}
```

### Поле времени

```javascript
{
    name: 'startTime',
    type: 'time',
    label: 'Время начала',
    required: true
}
```

## Валидация

Система автоматически валидирует поля на основе их конфигурации:

- **required** - проверка обязательности заполнения
- **minLength/maxLength** - проверка длины для text/textarea
- **min/max** - проверка диапазона для number
- **email** - проверка формата email
- **pattern** - можно расширить для custom-валидации

## Особенности реализации

### Модульность
Каждый модуль независим и имеет четкое API через публичные методы.

### Универсальность
Форма генерируется автоматически на основе JSON-конфигурации. Не нужно писать HTML.

### Расширяемость
Легко добавить:
- Новые типы полей (в form-renderer.js)
- Новые правила валидации (в validator.js)
- Новые типы заявок (в form-configs.js)
- Новые справочники (в data-service.js)

### Зависимости между полями
Поддерживается каскадная загрузка данных (например, выбор города → загрузка отделов этого города).

### Удобный UX
- Валидация при потере фокуса
- Визуальное выделение ошибок
- Плавные анимации
- Адаптивный дизайн

## Текущие реализованные формы

1. **Общая заявка** - универсальная форма с выбором типа и приоритета
2. **Техническая поддержка** - для IT-проблем
3. **Кадровый вопрос** - отпуска, справки, документы
4. **Запрос доступа** - доступ к системам
5. **Заказ оборудования** - запрос нового оборудования
6. **Бронирование переговорной** - резервирование помещений

## API модулей

### FormConfigs

```javascript
FormConfigs.getFormTypes()        // Получить все типы форм
FormConfigs.getFormConfig(id)     // Получить конфигурацию формы
FormConfigs.getFormType(id)       // Получить информацию о типе формы
```

### FormRenderer

```javascript
FormRenderer.init(fields, container)      // Инициализация
FormRenderer.render()                     // Рендеринг формы
FormRenderer.getFormData()                // Получение данных
FormRenderer.showErrors(errors)           // Отображение ошибок
FormRenderer.clearErrors()                // Очистка ошибок
FormRenderer.clearForm()                  // Очистка формы
FormRenderer.updateSelectOptions(name, options) // Обновление опций select
```

### Validator

```javascript
Validator.validateField(field, value)     // Валидация поля
Validator.validateForm(fields, formData)  // Валидация всей формы
Validator.hasErrors(errors)               // Проверка наличия ошибок
```

### DataService

```javascript
DataService.loadDictionary(name, dependsOn) // Загрузка справочника
DataService.submitForm(formData)            // Отправка формы
```

## Интеграция с backend

Для интеграции с реальным backend достаточно заменить методы в **data-service.js**:

```javascript
function loadDictionary(dictionaryName, dependsOnValue) {
    return fetch(`/api/dictionaries/${dictionaryName}?dependsOn=${dependsOnValue}`)
        .then(response => response.json());
}

function submitForm(formData) {
    return fetch('/api/requests', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(formData)
    }).then(response => response.json());
}
```

## Дальнейшее развитие

Возможные улучшения:

1. Добавить новые типы полей (checkbox, radio, file upload)
2. Добавить условную видимость полей
3. Добавить вычисляемые поля
4. Добавить мульти-селект
5. Добавить автокомплит
6. Добавить маски для ввода (например, телефон)
7. Добавить валидацию на стороне сервера
8. Добавить сохранение черновиков
9. Добавить историю заявок
10. Добавить уведомления

