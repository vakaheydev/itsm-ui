# 📋 Спецификация JSON-конфигурации форм

## Структура конфигурации формы

### Формат описания типа формы

```json
{
  "id": "unique-form-id",
  "name": "Отображаемое название формы",
  "description": "Краткое описание для пользователя",
  "icon": "📋"
}
```

### Формат описания поля формы

```json
{
  "name": "fieldName",
  "type": "field-type",
  "label": "Подпись поля",
  "placeholder": "Текст подсказки",
  "required": true|false,
  "minLength": 0,
  "maxLength": 100,
  "min": 0,
  "max": 100,
  "rows": 4,
  "dictionary": "dictionaryName",
  "dependsOn": "parentFieldName"
}
```

## Спецификация свойств полей

### Общие свойства (для всех типов)

| Свойство | Тип | Обязательное | Описание |
|----------|-----|--------------|----------|
| `name` | string | ✅ | Уникальное имя поля |
| `type` | string | ✅ | Тип поля (см. ниже) |
| `label` | string | ✅ | Текст подписи |
| `placeholder` | string | ❌ | Текст подсказки |
| `required` | boolean | ❌ | Обязательность (default: false) |

### Свойства для text, email

| Свойство | Тип | Описание |
|----------|-----|----------|
| `minLength` | number | Минимальная длина текста |
| `maxLength` | number | Максимальная длина текста |

**Пример:**
```json
{
  "name": "fullName",
  "type": "text",
  "label": "ФИО",
  "placeholder": "Введите полное имя",
  "required": true,
  "minLength": 3,
  "maxLength": 100
}
```

### Свойства для number

| Свойство | Тип | Описание |
|----------|-----|----------|
| `min` | number | Минимальное значение |
| `max` | number | Максимальное значение |

**Пример:**
```json
{
  "name": "age",
  "type": "number",
  "label": "Возраст",
  "placeholder": "Введите возраст",
  "required": true,
  "min": 18,
  "max": 100
}
```

### Свойства для textarea

| Свойство | Тип | Описание |
|----------|-----|----------|
| `minLength` | number | Минимальная длина текста |
| `maxLength` | number | Максимальная длина текста |
| `rows` | number | Количество строк (default: 4) |

**Пример:**
```json
{
  "name": "description",
  "type": "textarea",
  "label": "Описание",
  "placeholder": "Подробное описание",
  "required": true,
  "minLength": 10,
  "maxLength": 1000,
  "rows": 5
}
```

### Свойства для select

| Свойство | Тип | Описание |
|----------|-----|----------|
| `dictionary` | string | Название справочника |
| `dependsOn` | string | Имя родительского поля (optional) |

**Пример простого select:**
```json
{
  "name": "priority",
  "type": "select",
  "label": "Приоритет",
  "placeholder": "Выберите приоритет",
  "required": true,
  "dictionary": "priorities"
}
```

**Пример зависимого select:**
```json
{
  "name": "department",
  "type": "select",
  "label": "Отдел",
  "placeholder": "Выберите отдел",
  "required": true,
  "dictionary": "departments",
  "dependsOn": "city"
}
```

### Свойства для date

| Свойство | Тип | Описание |
|----------|-----|----------|
| - | - | Автоматически устанавливается min=today |

**Пример:**
```json
{
  "name": "startDate",
  "type": "date",
  "label": "Дата начала",
  "required": true
}
```

### Свойства для time

**Пример:**
```json
{
  "name": "startTime",
  "type": "time",
  "label": "Время начала",
  "required": true
}
```

## Типы полей

| Тип | HTML тип | Описание |
|-----|----------|----------|
| `text` | input[type="text"] | Обычное текстовое поле |
| `email` | input[type="email"] | Email с валидацией |
| `number` | input[type="number"] | Числовое поле |
| `date` | input[type="date"] | Выбор даты |
| `time` | input[type="time"] | Выбор времени |
| `select` | select | Выпадающий список |
| `textarea` | textarea | Многострочный текст |

## Формат справочников

### Простой справочник (массив)

```javascript
dictionaryName: [
  { id: 'value1', name: 'Опция 1' },
  { id: 'value2', name: 'Опция 2' },
  { id: 'value3', name: 'Опция 3' }
]
```

### Зависимый справочник (объект)

```javascript
dictionaryName: {
  'parent-value-1': [
    { id: 'child1', name: 'Дочерний элемент 1' },
    { id: 'child2', name: 'Дочерний элемент 2' }
  ],
  'parent-value-2': [
    { id: 'child3', name: 'Дочерний элемент 3' }
  ]
}
```

## Полный пример формы

```javascript
// Описание типа формы
{
  id: 'feedback',
  name: 'Форма обратной связи',
  description: 'Отправьте нам ваши предложения',
  icon: '💬'
}

// Конфигурация полей
const feedbackFormConfig = [
  {
    name: 'name',
    type: 'text',
    label: 'Ваше имя',
    placeholder: 'Введите имя',
    required: true,
    minLength: 2,
    maxLength: 50
  },
  {
    name: 'email',
    type: 'email',
    label: 'Email для ответа',
    placeholder: 'example@mail.com',
    required: true,
    maxLength: 100
  },
  {
    name: 'category',
    type: 'select',
    label: 'Категория обращения',
    placeholder: 'Выберите категорию',
    required: true,
    dictionary: 'feedbackCategories'
  },
  {
    name: 'rating',
    type: 'number',
    label: 'Оценка (1-10)',
    required: false,
    min: 1,
    max: 10
  },
  {
    name: 'message',
    type: 'textarea',
    label: 'Ваше сообщение',
    placeholder: 'Напишите ваши предложения или замечания',
    required: true,
    minLength: 10,
    maxLength: 2000,
    rows: 6
  }
];

// Справочники
const mockData = {
  feedbackCategories: [
    { id: 'bug', name: 'Сообщить об ошибке' },
    { id: 'feature', name: 'Предложить функцию' },
    { id: 'question', name: 'Задать вопрос' },
    { id: 'other', name: 'Другое' }
  ]
};

// Регистрация
const formConfigsMap = {
  'feedback': feedbackFormConfig
};
```

## Правила валидации

### Автоматическая валидация

| Условие | Правило | Сообщение |
|---------|---------|-----------|
| `required: true` | Поле не пустое | "Это поле обязательно для заполнения" |
| `minLength: N` | Длина >= N | "Минимальная длина: N символов" |
| `maxLength: N` | Длина <= N | "Максимальная длина: N символов" |
| `min: N` | Значение >= N | "Минимальное значение: N" |
| `max: N` | Значение <= N | "Максимальное значение: N" |
| `type: 'email'` | Email формат | "Введите корректный email адрес" |
| `type: 'number'` | Число | "Введите корректное число" |

### Когда происходит валидация

1. **При потере фокуса (blur)** - валидация отдельного поля
2. **При отправке формы** - валидация всех полей
3. **При вводе (input)** - очистка ошибки

## Формат данных при отправке

```javascript
{
  // Данные полей
  "fieldName1": "value1",
  "fieldName2": "value2",
  
  // Метаданные (добавляются автоматически)
  "_formType": "form-id",
  "_formName": "Название формы"
}
```

## Расширение системы

### Добавление нового типа поля

1. **В form-renderer.js** добавить функцию создания:
```javascript
function createMyCustomField(field) {
  const element = document.createElement('input');
  element.type = 'text';
  // ... кастомная логика
  return element;
}
```

2. **В createFormGroup()** добавить обработку:
```javascript
if (field.type === 'my-custom') {
  fieldElement = createMyCustomField(field);
}
```

3. **В validator.js** добавить валидацию (если нужно):
```javascript
if (field.type === 'my-custom') {
  // кастомная валидация
}
```

### Добавление кастомной валидации

```javascript
// В конфигурации поля
{
  name: 'inn',
  type: 'text',
  label: 'ИНН',
  customValidation: 'validateInn'
}

// В validator.js
if (field.customValidation === 'validateInn') {
  if (!/^\d{10}$|^\d{12}$/.test(value)) {
    return 'ИНН должен содержать 10 или 12 цифр';
  }
}
```

## Лучшие практики

### ✅ Рекомендуется

- Использовать говорящие имена полей (`fullName`, а не `field1`)
- Группировать связанные поля рядом
- Использовать placeholder для подсказок
- Указывать реальные ограничения (min/max)
- Делать обязательными только действительно необходимые поля

### ❌ Не рекомендуется

- Слишком длинные формы (>15 полей)
- Все поля обязательные
- Отсутствие placeholder
- Слишком жёсткие ограничения
- Неинформативные названия полей

## Примеры готовых конфигураций

Смотрите файл **EXAMPLES.js** для:
- Форм с каскадными зависимостями
- Форм с кастомной валидацией
- Сложных многоуровневых форм
- Форм с условной логикой (расширение)

