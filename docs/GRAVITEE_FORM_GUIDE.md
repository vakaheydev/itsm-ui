# 🔌 Форма "Подписка в Gravitee" - Документация

## ✅ Что реализовано

Создана новая форма **"Подписка в Gravitee"** с поддержкой множественного выбора методов API.

---

## 🎯 Поля формы

### 1. **Окружение** (select)
- Тип: Константный список
- Значения: TEST, REGRESS, PROD
- Обязательное: Да

### 2. **Зона** (select)
- Тип: Константный список
- Значения: INT, EXT, INT + EXT
- Обязательное: Да

### 3. **API** (select с поиском)
- Тип: Справочник
- Источник: `api`
- Опции:
  - API Пользователей (UserAPI)
  - API Заказов (OrderAPI)
  - API Платежей (PaymentAPI)
  - API Уведомлений (NotificationAPI)
  - API Справочников (DictionaryAPI)
- Обязательное: Да

### 4. **Методы API** (multiselect) ⭐ НОВОЕ!
- Тип: **Множественный выбор с поиском**
- Источник: `api_methods` (зависит от выбранного API)
- Можно выбрать несколько методов
- Обязательное: Да

### 5. **Способ аутентификации** (select)
- Тип: Константный список
- Значения: JWT, API_KEY, KEY_LESS
- Обязательное: Да

---

## 🆕 Новый тип поля: MultiSelect

### Что это?

**MultiSelect** - это кастомный компонент для выбора нескольких значений из списка.

### Возможности:

✅ **Множественный выбор** - можно выбрать несколько опций  
✅ **Поиск** - встроенная фильтрация  
✅ **Тэги** - выбранные элементы отображаются как тэги  
✅ **Удаление** - клик по × удаляет тэг  
✅ **Checkboxes** - визуальная индикация выбора  
✅ **Зависимости** - поддержка `dependsOn`  

---

## 🏗️ Структура MultiSelect

```html
<div class="custom-multiselect">
    <!-- Скрытое поле со значениями через запятую -->
    <input type="hidden" id="api_methods" value="get-user,create-user">
    
    <!-- Контейнер для выбранных тэгов -->
    <div class="multiselect-tags">
        <span class="multiselect-tag">
            GET /users/{id}
            <span class="multiselect-tag-remove">×</span>
        </span>
        <span class="multiselect-tag">
            POST /users
            <span class="multiselect-tag-remove">×</span>
        </span>
    </div>
    
    <!-- Поле поиска -->
    <input type="text" class="multiselect-input" placeholder="Выберите методы">
    
    <!-- Стрелка -->
    <span class="multiselect-arrow">▼</span>
    
    <!-- Список с checkbox'ами -->
    <div class="multiselect-options">
        <div class="multiselect-option">
            <input type="checkbox" id="api_methods-get-user" checked>
            <label>GET /users/{id} - Получить пользователя</label>
        </div>
        <!-- ... -->
    </div>
</div>
```

---

## 🎨 Визуальные элементы

### Тэги (Tags)

Выбранные значения отображаются как **цветные тэги** над полем ввода:

```
┌─────────────────────────────────────┐
│ ┌─────────────┐ ┌─────────────┐    │ ← Тэги
│ │ Method 1  × │ │ Method 2  × │    │
│ └─────────────┘ └─────────────┘    │
│                                     │
│ Выберите методы...            ▼    │ ← Поле поиска
└─────────────────────────────────────┘
```

### Список с чекбоксами

При клике открывается список с чекбоксами:

```
┌─────────────────────────────────────┐
│ ☑ GET /users/{id} - Получить        │
│ ☑ POST /users - Создать             │
│ ☐ PUT /users/{id} - Обновить        │
│ ☐ DELETE /users/{id} - Удалить      │
└─────────────────────────────────────┘
```

---

## 🔧 Конфигурация

### Объявление в form-configs.js

```javascript
{
    name: 'api_methods',
    type: 'multiselect',              // ← Тип поля
    label: 'Методы API',
    placeholder: 'Выберите методы (можно несколько)',
    required: true,
    dictionary: 'api_methods',
    dependsOn: 'api'                  // ← Зависит от API
}
```

### Справочник в data-service.js

```javascript
api_methods: {
    'api-1': [  // UserAPI
        { id: 'get-user', name: 'GET /users/{id} - Получить пользователя' },
        { id: 'create-user', name: 'POST /users - Создать пользователя' },
        // ...
    ],
    'api-2': [  // OrderAPI
        { id: 'get-order', name: 'GET /orders/{id} - Получить заказ' },
        // ...
    ]
}
```

---

## 🔄 Как работает

### Сценарий использования:

```
1. Пользователь выбирает API = "UserAPI"
   ↓
2. Загружаются методы для UserAPI
   ↓
3. Пользователь кликает на поле "Методы"
   ↓
4. Открывается список с чекбоксами
   ↓
5. Пользователь ставит галочки на нужных методах
   ↓
6. Выбранные методы появляются как тэги
   ↓
7. Скрытый input содержит: "get-user,create-user,delete-user"
```

### Поиск в списке:

```
1. Пользователь печатает "POST"
   ↓
2. Список фильтруется → показываются только методы с "POST"
   ↓
3. Пользователь выбирает нужные
```

### Удаление тэга:

```
1. Пользователь кликает на × в тэге
   ↓
2. Тэг исчезает
   ↓
3. Снимается галочка в списке
   ↓
4. Значение удаляется из скрытого input
```

---

## 💾 Формат данных

### В форме (скрытый input):

```javascript
"get-user,create-user,list-users"  // Через запятую
```

### При отправке (getFormData):

```javascript
{
    environment: "TEST",
    zone: "INT",
    api: "api-1",
    api_methods: "get-user,create-user,list-users",  // ← Строка
    auth_method: "JWT"
}
```

### На сервере (разбор):

```javascript
// JavaScript
const methods = formData.api_methods.split(',');
// ["get-user", "create-user", "list-users"]

// Java
String[] methods = formData.getApiMethods().split(",");
// ["get-user", "create-user", "list-users"]
```

---

## 🎨 CSS классы

| Класс | Назначение |
|-------|------------|
| `.custom-multiselect` | Контейнер компонента |
| `.multiselect-tags` | Контейнер тэгов |
| `.multiselect-tag` | Один тэг |
| `.multiselect-tag-remove` | Кнопка × удаления |
| `.multiselect-input` | Поле поиска |
| `.multiselect-arrow` | Стрелка-индикатор |
| `.multiselect-options` | Список опций |
| `.multiselect-option` | Одна опция с checkbox |

---

## 📝 API функции

### FormRenderer.updateMultiSelectOptions()

```javascript
FormRenderer.updateMultiSelectOptions('api_methods', [
    { id: 'get-user', name: 'GET /users/{id}' },
    { id: 'create-user', name: 'POST /users' }
]);
```

**Что делает:**
1. Очищает старый список
2. Создаёт checkbox для каждой опции
3. Устанавливает обработчики
4. Восстанавливает выбранные значения

### toggleMultiSelectOption()

```javascript
toggleMultiSelectOption('api_methods', 'get-user', 'GET /users/{id}', true);
```

**Что делает:**
1. Добавляет/удаляет значение из скрытого input
2. Обновляет тэги
3. Триггерит событие `change`

### updateMultiSelectTags()

```javascript
updateMultiSelectTags('api_methods');
```

**Что делает:**
1. Читает выбранные значения
2. Создаёт тэги для каждого
3. Добавляет кнопки удаления

---

## 🔗 Зависимости между полями

### Конфигурация:

```javascript
{
    name: 'api',
    type: 'select',
    dictionary: 'api'
},
{
    name: 'api_methods',
    type: 'multiselect',
    dictionary: 'api_methods',
    dependsOn: 'api'  // ← Зависит от API
}
```

### Работа:

```
Выбрали API = "UserAPI"
    ↓
handleDependentFields('api', 'api-1')
    ↓
DataService.loadDictionary('api_methods', 'api-1')
    ↓
FormRenderer.updateMultiSelectOptions('api_methods', data)
    ↓
Список методов обновился для UserAPI
```

---

## 🎯 Константные справочники

Добавлены 3 константных справочника (одинаковы для всех заявок):

### graviteeEnvironments
```javascript
[
    { id: 'TEST', name: 'TEST' },
    { id: 'REGRESS', name: 'REGRESS' },
    { id: 'PROD', name: 'PROD' }
]
```

### graviteeZones
```javascript
[
    { id: 'INT', name: 'INT' },
    { id: 'EXT', name: 'EXT' },
    { id: 'INT_EXT', name: 'INT + EXT' }
]
```

### graviteeAuthMethods
```javascript
[
    { id: 'JWT', name: 'JWT' },
    { id: 'API_KEY', name: 'API_KEY' },
    { id: 'KEY_LESS', name: 'KEY_LESS' }
]
```

---

## 🚀 Использование в других формах

### Создание multiselect поля:

```javascript
{
    name: 'technologies',
    type: 'multiselect',
    label: 'Технологии',
    placeholder: 'Выберите технологии',
    required: false,
    dictionary: 'technologies'
}
```

### Добавление справочника:

```javascript
// В data-service.js
technologies: [
    { id: 'js', name: 'JavaScript' },
    { id: 'java', name: 'Java' },
    { id: 'python', name: 'Python' },
    { id: 'csharp', name: 'C#' }
]
```

### Готово!

Поле автоматически:
- Отобразится с поиском
- Позволит выбрать несколько значений
- Покажет выбранные как тэги
- Сохранит значения через запятую

---

## ✨ Итого

### Создана форма "Подписка в Gravitee" с полями:
✅ Окружение (TEST/REGRESS/PROD)  
✅ Зона (INT/EXT/INT+EXT)  
✅ API (справочник)  
✅ Методы API (**множественный выбор** с зависимостью от API)  
✅ Способ аутентификации (JWT/API_KEY/KEY_LESS)  

### Реализован новый тип поля **multiselect**:
✅ Множественный выбор  
✅ Поиск по опциям  
✅ Тэги для выбранных  
✅ Удаление тэгов  
✅ Поддержка зависимостей  

### Добавлены 3 константных справочника:
✅ graviteeEnvironments  
✅ graviteeZones  
✅ graviteeAuthMethods  

**Готово к использованию! 🎉**

