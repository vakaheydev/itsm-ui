# 🔍 Кастомный Select с поиском - Документация

## ✅ Что изменилось

Стандартный HTML `<select>` заменён на **кастомный компонент с встроенным поиском**.

---

## 🎯 Возможности

### 1. **Поиск по опциям**
- Начните печатать в поле → список автоматически фильтруется
- Поиск работает по всему тексту опции
- Регистр не важен (case-insensitive)

### 2. **Визуальное оформление**
- Современный дизайн с плавными анимациями
- Стрелка-индикатор состояния (открыто/закрыто)
- Подсветка при наведении на опции
- Кастомный скроллбар

### 3. **Удобство использования**
- Клик по полю → список открывается
- Клик вне списка → закрывается
- Сообщение "Ничего не найдено" при пустом результате
- Сохранение выбранного значения

---

## 🏗️ Структура компонента

```html
<div class="custom-select">
    <!-- Скрытое поле для хранения значения -->
    <input type="hidden" id="city" name="city" value="">
    
    <!-- Поле для поиска и отображения выбранного -->
    <input type="text" class="form-control custom-select-input" 
           placeholder="Выберите город">
    
    <!-- Стрелка-индикатор -->
    <span class="custom-select-arrow">▼</span>
    
    <!-- Выпадающий список опций -->
    <div class="custom-select-options">
        <div class="custom-select-option" data-value="msk">Москва</div>
        <div class="custom-select-option" data-value="spb">Санкт-Петербург</div>
        <!-- ... -->
    </div>
    
    <!-- Сообщение "Нет результатов" -->
    <div class="custom-select-no-results">Ничего не найдено</div>
</div>
```

---

## ⚙️ Как работает

### При открытии формы:
1. Создаётся структура кастомного select
2. Загружаются опции из справочника
3. Устанавливаются обработчики событий

### При вводе текста:
1. Пользователь печатает "моск"
2. Срабатывает событие `input`
3. Вызывается `filterOptions()` с запросом "моск"
4. Опции фильтруются (показывается "Москва", скрывается остальное)

### При выборе:
1. Пользователь кликает на "Москва"
2. Вызывается `selectOption('city', 'msk', 'Москва')`
3. Скрытый input получает значение `'msk'`
4. Видимый input показывает `'Москва'`
5. Список закрывается
6. Триггерится событие `change` для зависимых полей

---

## 📝 Код обработки

### Фильтрация опций

```javascript
function filterOptions(fieldName, searchQuery) {
    const options = document.querySelectorAll('.custom-select-option');
    const query = searchQuery.toLowerCase();
    
    options.forEach(function(option) {
        const text = option.textContent.toLowerCase();
        if (text.indexOf(query) !== -1) {
            option.style.display = 'block';  // Показать
        } else {
            option.style.display = 'none';   // Скрыть
        }
    });
}
```

### Выбор опции

```javascript
function selectOption(fieldName, value, text) {
    // Сохраняем ID в скрытом поле
    hiddenInput.value = value;  // 'msk'
    
    // Показываем текст пользователю
    searchInput.value = text;   // 'Москва'
    
    // Закрываем список
    optionsList.classList.remove('show');
    
    // Уведомляем о change для зависимых полей
    hiddenInput.dispatchEvent(new Event('change'));
}
```

---

## 🎨 CSS классы

| Класс | Назначение |
|-------|------------|
| `.custom-select` | Контейнер компонента |
| `.custom-select-input` | Поле поиска/отображения |
| `.custom-select-arrow` | Стрелка-индикатор |
| `.custom-select-options` | Контейнер списка опций |
| `.custom-select-options.show` | Открытый список |
| `.custom-select-option` | Одна опция в списке |
| `.custom-select-option:hover` | Подсветка при наведении |
| `.custom-select-no-results` | Сообщение "Нет результатов" |

---

## 🔗 Совместимость с существующим кодом

### ✅ Работает как раньше:

1. **Зависимые поля**
   ```javascript
   {
       name: 'department',
       type: 'select',
       dependsOn: 'city'  // ← Работает!
   }
   ```

2. **Валидация**
   ```javascript
   Validator.validateField(field, value)  // ← Работает!
   ```

3. **Получение данных**
   ```javascript
   FormRenderer.getFormData()  // ← Работает!
   ```

4. **Обновление опций**
   ```javascript
   FormRenderer.updateSelectOptions('city', data)  // ← Работает!
   ```

---

## 🚀 Преимущества

### Для пользователя:
- ✅ Быстрый поиск в больших списках
- ✅ Не нужно скроллить сотни опций
- ✅ Удобнее стандартного select
- ✅ Приятная анимация

### Для разработчика:
- ✅ API осталось прежним
- ✅ Никаких изменений в конфигурации
- ✅ Автоматически работает для всех select
- ✅ Легко кастомизировать через CSS

---

## 📊 Пример использования

### Конфигурация (без изменений)

```javascript
{
    name: 'city',
    type: 'select',          // ← Просто указываем тип
    label: 'Город',
    placeholder: 'Выберите город',
    required: true,
    dictionary: 'cities'
}
```

### Загрузка данных (без изменений)

```javascript
DataService.loadDictionary('cities')
    .then(function(data) {
        FormRenderer.updateSelectOptions('city', data);
    });
```

### Результат

Автоматически создаётся кастомный select с:
- Поиском
- Красивым дизайном
- Плавными анимациями

---

## 🛠️ Настройка внешнего вида

### Изменить высоту списка

```css
.custom-select-options.show {
    max-height: 350px;  /* По умолчанию 250px */
}
```

### Изменить цвет подсветки

```css
.custom-select-option:hover {
    background-color: #your-color;
    color: #your-text-color;
}
```

### Изменить стрелку

```css
.custom-select-arrow {
    content: '▼';  /* Любой символ или иконка */
}
```

---

## 🔍 Отладка

### Проверить что выбрано

```javascript
// В консоли браузера
const hiddenInput = document.getElementById('city');
console.log(hiddenInput.value);  // ID значения

const searchInput = document.querySelector('[data-field-name="city"]');
console.log(searchInput.value);  // Текст для пользователя
```

### Проверить опции

```javascript
const options = document.getElementById('city-options');
console.log(options.innerHTML);
```

---

## ✨ Итого

Теперь все поля типа `select` в проекте автоматически:
- 🔍 Имеют встроенный поиск
- 🎨 Красиво оформлены
- 📱 Удобны в использовании
- ⚡ Работают быстро

**Никаких изменений в конфигурации не требуется!**

