# Быстрый старт: Добавление новой формы

## Пример: Добавление формы "Заявка на отгул"

### 1. Откройте form-configs.js

### 2. Добавьте новый тип в массив formTypes:

```javascript
const formTypes = [
    // ...существующие типы
    {
        id: 'day-off',
        name: 'Заявка на отгул',
        description: 'Запрос отгула или отпуска за свой счет',
        icon: '🏖️'
    }
];
```

### 3. Создайте конфигурацию формы:

```javascript
// После существующих конфигураций добавьте:
const dayOffFormConfig = [
    {
        name: 'fullName',
        type: 'text',
        label: 'ФИО',
        placeholder: 'Введите ваше полное имя',
        required: true,
        minLength: 3,
        maxLength: 100
    },
    {
        name: 'employeeId',
        type: 'text',
        label: 'Табельный номер',
        placeholder: 'Ваш табельный номер',
        required: true,
        maxLength: 20
    },
    {
        name: 'department',
        type: 'text',
        label: 'Отдел',
        placeholder: 'Название вашего отдела',
        required: true,
        maxLength: 100
    },
    {
        name: 'dayOffType',
        type: 'select',
        label: 'Тип отгула',
        placeholder: 'Выберите тип',
        required: true,
        dictionary: 'dayOffTypes'
    },
    {
        name: 'startDate',
        type: 'date',
        label: 'Дата начала',
        required: true
    },
    {
        name: 'endDate',
        type: 'date',
        label: 'Дата окончания',
        required: true
    },
    {
        name: 'reason',
        type: 'textarea',
        label: 'Причина',
        placeholder: 'Укажите причину запроса отгула',
        required: true,
        minLength: 10,
        maxLength: 500,
        rows: 4
    }
];
```

### 4. Добавьте в маппинг formConfigsMap:

```javascript
const formConfigsMap = {
    general: generalFormConfig,
    technical: technicalFormConfig,
    hr: hrFormConfig,
    access: accessFormConfig,
    equipment: equipmentFormConfig,
    meeting: meetingFormConfig,
    'day-off': dayOffFormConfig  // <- Добавьте эту строку
};
```

### 5. Откройте data-service.js и добавьте справочник:

```javascript
const mockData = {
    // ...существующие справочники
    dayOffTypes: [
        { id: 'personal', name: 'Личные обстоятельства' },
        { id: 'family', name: 'Семейные обстоятельства' },
        { id: 'medical', name: 'Медицинские причины' },
        { id: 'education', name: 'Учеба/повышение квалификации' },
        { id: 'other', name: 'Другое' }
    ]
};
```

### 6. Готово!

Откройте index.html в браузере - новая форма "Заявка на отгул" появится на главном экране.

## Это всё что нужно!

Система автоматически:
- ✅ Отобразит кнопку выбора формы
- ✅ Создаст все поля формы
- ✅ Настроит валидацию
- ✅ Загрузит справочники
- ✅ Обработает отправку
- ✅ Покажет ошибки валидации

## Продвинутые возможности

### Зависимые поля

Если нужно, чтобы одно поле зависело от другого:

```javascript
{
    name: 'office',
    type: 'select',
    label: 'Офис',
    required: true,
    dictionary: 'offices'
},
{
    name: 'building',
    type: 'select',
    label: 'Корпус',
    required: true,
    dictionary: 'buildings',
    dependsOn: 'office'  // Зависит от выбранного офиса
}
```

В data-service.js:

```javascript
buildings: {
    'office-1': [
        { id: 'a', name: 'Корпус А' },
        { id: 'b', name: 'Корпус Б' }
    ],
    'office-2': [
        { id: 'c', name: 'Корпус С' }
    ]
}
```

### Условная обязательность

Можно легко расширить, добавив в конфигурацию:

```javascript
{
    name: 'otherReason',
    type: 'textarea',
    label: 'Укажите причину',
    required: false,  // Можно добавить логику условной обязательности
    requiredIf: { field: 'dayOffType', value: 'other' }
}
```

### Кастомная валидация

В validator.js можно добавить свои правила:

```javascript
// Проверка формата ИНН
if (field.validateInn && value) {
    if (!/^\d{10}$|^\d{12}$/.test(value)) {
        return 'Введите корректный ИНН (10 или 12 цифр)';
    }
}
```

